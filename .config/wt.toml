[pre-start]
# Copy selected gitignored files into new worktrees so they start warm.
# pre-start runs blocking on worktree creation (renamed from the
# deprecated post-create hook in worktrunk 0.39); it doesn't fire on
# every subsequent start, so node_modules etc. aren't re-copied.
copy = "wt step copy-ignored"
